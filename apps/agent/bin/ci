#!/usr/bin/env bash
# Agent CI: go vet + go build + go test.
set -euo pipefail

cd "$(dirname "$0")/.."

echo "==> go vet"
go vet ./...

echo "==> go build"
go build ./...

echo "==> go test"
go test ./...

echo "OK"
