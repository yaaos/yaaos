# This module has been retired.
# The `build_invocation` helper and `InvocationMode` were removed when the
# legacy multi-mode dispatch surface was retired. `CodingAgentPlugin` now
# exposes only `build_invocation(Invocation) -> InvokeCodingAgent` and
# `parse_result(payload) -> RunResult`.
__all__: list[str] = []
