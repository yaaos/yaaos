# This test file has been retired — the `build_invocation` module-level
# helper and `InvocationMode` were removed. Plugin-level `build_invocation`
# tests live in `apps/backend/app/plugins/claude_code/test/test_build_invocation_method.py`.
