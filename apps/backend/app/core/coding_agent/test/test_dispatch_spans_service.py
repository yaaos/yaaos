# This test file has been retired — the `review` dispatch wrapper was removed
# from `core/coding_agent` when the legacy multi-mode dispatch surface was
# retired. `dispatch_invocation` span coverage lives in
# `test_dispatch_invocation_service.py`.
